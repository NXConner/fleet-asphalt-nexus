import React from 'react';

export const InventoryList = ({ items, onSelect }: { items?: any[], onSelect?: (item: any) => void }) => {
  return <div>Inventory List Placeholder</div>;
};

export default InventoryList; 