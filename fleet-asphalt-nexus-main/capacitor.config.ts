import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fleet.asphalt.nexus',
  appName: 'fleet-asphalt-nexus',
  webDir: 'dist'
};

export default config;
